Build uboot use the build.sh

Step 1: 
Download toolchain tool and extract to the fold.
Define CROSS_COMPILE_PATH to extract fold at build.sh.
https://drive.google.com/drive/folders/183Me7tEWx8NbZA9SSSy5UBg80MqBo1gb?usp=sharing

Step 2:
Define the CROSS_COMPILE_TOOL at build.sh
Define the CROSS_COMPILE_GCC at build.sh

Step 3:
Select TARGET_CUSTOMER at build.sh

Step 4:
build uboot by 'build.sh all' command.

Step 5:
You can find uImage, modules, fireware and *.dtb at out fold if build success.

